export default defineEventHandler(async (event) => {
  //   const { identity, access, refresh } = useRuntimeConfig().tokens;
  //   console.log(
  //     "identity (%s) | access  (%s) | refresh  (%s)",
  //     !!getCookie(event, identity),
  //     !!getCookie(event, access),
  //     !!getCookie(event, refresh),
  //   );
});
