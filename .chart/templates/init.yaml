# auth-chart/templates/init-jobs.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: database-sql-scripts
data:
  migrations.sql: |
    -- Twój schemat bazy (CREATE SCHEMA IF NOT EXISTS auth; itd.)
    CREATE SCHEMA IF NOT EXISTS auth;
  seeding.sql: |
    -- Twoje cyber-role (INSERT INTO auth.roles... itp.)
    SELECT 1;
---
apiVersion: batch/v1
kind: Job
metadata:
  name: auth-database-bootstrap
spec:
  template:
    spec:
      restartPolicy: OnFailure
      initContainers:
        - name: cluster-init
          image: { { .Values.database.image } }
          command:
            [
              "/cockroach/cockroach",
              "init",
              "--insecure",
              "--host=auth-database-0.auth-database:26357",
            ]
        - name: cluster-migrate
          image: { { .Values.database.image } }
          command:
            [
              "sh",
              "-c",
              "/cockroach/cockroach sql --insecure --host=auth-database-0.auth-database:26257 --file=/scripts/migrations.sql",
            ]
          volumeMounts:
            - name: sql-volume
              mountPath: /scripts
              - name: cluster-creator
          image: {{ .Values.cache.image }}
          command:
            - "sh"
            - "-c"
            - |
              echo "Czekam aż pody Valkey będą gotowe..."
              # Pętla sprawdza, czy ostatni pod (auth-cache-5) odpowiada na sieci
              until nc -z auth-cache-5.auth-cache-headless 6379; do
                sleep 2
              done

              echo "Pody wstały. Pobieram IP wszystkich podów..."
              # Pobieramy adresy IP podów na podstawie wewnętrznego DNS K8s
              IPS=""
              for i in 0 1 2 3 4 5; do
                IP=$(getent hosts auth-cache-$i.auth-cache-headless | awk '{ print $1 }')
                IPS="$IPS $IP:6379"
              done

              echo "Uruchamiam tworzenie klastra z IP: $IPS"
              # Komenda automatycznie tworzy klaster i paruje Mastery z replikami (--cluster-replicas 1)
              echo "yes" | valkey-cli --cluster create $IPS --cluster-replicas 1
        containers:
        - name: cluster-seed
          image: { { .Values.database.image } }
          command:
            [
              "sh",
              "-c",
              "/cockroach/cockroach sql --insecure --host=auth-database-0.auth-database:26257 --file=/scripts/seeding.sql",
            ]
          volumeMounts:
            - name: sql-volume
              mountPath: /scripts
      volumes:
        - name: sql-volume
          configMap:
            name: database-sql-scripts
